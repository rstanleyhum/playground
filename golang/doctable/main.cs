using System.IO;
using System.IO.Compression;


class MyGZipCompress
{
    public static byte[] CompressBuffer(byte[] byteArray)
    {
        MemoryStream strm = new MemoryStream();
        GZipStream
    }
}
